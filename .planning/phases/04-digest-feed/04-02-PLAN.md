---
phase: 04-digest-feed
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/page.tsx
  - src/components/filter-bar.tsx
  - src/app/item/[id]/page.tsx
autonomous: false
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04
  - FEED-05
  - FEED-06
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Visiting the homepage shows today's government changes with no loading spinner"
    - "User can filter the feed by type and see results update in the URL"
    - "User can filter the feed by topic and see results update in the URL"
    - "Clicking an item opens a detail page with what changed, who it affects, why it matters"
    - "Detail page links to the official government source"
    - "App works on mobile and desktop without broken layouts"
    - "Dark mode toggle works on feed and detail pages"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with feed and filter controls"
      min_lines: 30
    - path: "src/components/filter-bar.tsx"
      provides: "Client component for type/topic filter dropdowns"
      contains: "use client"
    - path: "src/app/item/[id]/page.tsx"
      provides: "Detail page with structured summary breakdown"
      min_lines: 40
  key_links:
    - from: "src/app/page.tsx"
      to: "src/features/feed/queries.ts"
      via: "getFeedItems() call in async server component"
      pattern: "getFeedItems"
    - from: "src/app/page.tsx"
      to: "src/components/filter-bar.tsx"
      via: "FilterBar rendered with activeType/activeTopic props"
      pattern: "FilterBar"
    - from: "src/app/page.tsx"
      to: "src/components/feed-item-card.tsx"
      via: "FeedItemCard rendered for each feed item"
      pattern: "FeedItemCard"
    - from: "src/components/filter-bar.tsx"
      to: "URL searchParams"
      via: "router.replace() updates URL"
      pattern: "router\\.replace"
    - from: "src/app/item/[id]/page.tsx"
      to: "src/features/feed/queries.ts"
      via: "getItemById() call"
      pattern: "getItemById"
    - from: "src/app/item/[id]/page.tsx"
      to: "src/features/summarization/schema.ts"
      via: "SummarySchema.safeParse for summary validation"
      pattern: "SummarySchema"
---

<objective>
Build the two page routes (homepage feed + item detail) and the filter bar client component — the actual user-facing product.

Purpose: This is the first time PatchNotes becomes a usable product. A person can visit the URL, see government changes, filter them, and read detailed breakdowns.
Output: Working homepage with filterable feed and detail page with structured patch-notes layout.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-digest-feed/04-RESEARCH.md
@.planning/phases/04-digest-feed/04-01-SUMMARY.md

<interfaces>
<!-- From plan 01 — contracts the executor consumes -->

From src/features/feed/types.ts (created in plan 01):
```typescript
export type FeedItem = PolicyItem & { parsedSummary: SummaryOutput | null };
export function parseSummary(raw: string | null): SummaryOutput | null;
export interface FeedFilters { type?: string; topic?: string; }
```

From src/features/feed/queries.ts (created in plan 01):
```typescript
export async function getFeedItems(filters?: FeedFilters): Promise<PolicyItem[]>;
export async function getItemById(id: string): Promise<PolicyItem | null>;
```

From src/components/feed-item-card.tsx (created in plan 01):
```typescript
// Server component — accepts item + parsedSummary as props
export function FeedItemCard({ item, parsedSummary }: { item: PolicyItem; parsedSummary: SummaryOutput | null }): JSX.Element;
```

From src/components/severity-badge.tsx (created in plan 01):
```typescript
export function SeverityBadge({ severity }: { severity: SummaryOutput['severity'] }): JSX.Element;
```

From src/components/type-badge.tsx (created in plan 01):
```typescript
export function TypeBadge({ type }: { type: string }): JSX.Element;
```

From src/features/summarization/schema.ts (existing):
```typescript
export const SummarySchema = z.object({
  headline: z.string(),
  whatChanged: z.string(),
  whoAffected: z.string(),
  whyItMatters: z.string(),
  severity: z.enum(['broad_national', 'moderate_regional', 'narrow_administrative']),
});
export type SummaryOutput = z.infer<typeof SummarySchema>;
```

From src/components/ui/select.tsx (installed in plan 01):
```typescript
export { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from shadcn;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage feed page and FilterBar client component</name>
  <files>src/app/page.tsx, src/components/filter-bar.tsx</files>
  <action>
1. Create `src/components/filter-bar.tsx` as a `"use client"` component:
   - Import `useRouter`, `usePathname` from `next/navigation`
   - Import `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` from `@/components/ui/select`
   - Props: `{ activeType?: string; activeTopic?: string }`
   - TYPE_OPTIONS: `[{ value: '', label: 'All Types' }, { value: 'executive_order', label: 'Executive Orders' }, { value: 'bill', label: 'Bills' }, { value: 'rule', label: 'Regulations' }, { value: 'proposed_rule', label: 'Proposed Rules' }]`
   - TOPIC_OPTIONS: `[{ value: '', label: 'All Topics' }, { value: 'healthcare', label: 'Healthcare' }, { value: 'taxes', label: 'Taxes' }, { value: 'immigration', label: 'Immigration' }, { value: 'environment', label: 'Environment' }, { value: 'defense', label: 'Defense' }, { value: 'education', label: 'Education' }, { value: 'economy', label: 'Economy' }]`
   - `updateFilter(key: 'type' | 'topic', value: string)`: Build URLSearchParams preserving the other filter, call `router.replace(query ? pathname + '?' + query : pathname, { scroll: false })`
   - If shadcn Select has issues with empty string value, use `value={activeType ?? undefined}` and a sentinel value `"all"` mapped to empty string in updateFilter
   - Render two Select dropdowns side by side in a flex-wrap container

2. Replace `src/app/page.tsx` entirely — the splash page becomes the feed:
   - Make it an async server component: `export default async function HomePage({ searchParams }: { searchParams: Promise<{ type?: string; topic?: string }> })`
   - `await searchParams` to get type and topic (CRITICAL: searchParams is a Promise in Next.js 15+)
   - Call `getFeedItems({ type, topic })` from `@/features/feed/queries`
   - For each item, call `parseSummary(item.summary)` from `@/features/feed/types`
   - Layout structure:
     - Sticky header: "PatchNotes" title on left, tagline "A changelog for your government" as subtitle, ThemeToggle on right. Use `bg-background/80 backdrop-blur` for frosted glass effect.
     - Below header: FilterBar with activeType and activeTopic props
     - Feed: `max-w-2xl mx-auto` container, vertical stack of FeedItemCard components with gap-4
     - Empty state: "No items match the selected filters." centered in muted text when items.length === 0
   - Remove the `DbStatus` import and the old splash page content entirely
   - Use semantic Tailwind classes for dark mode: `bg-background`, `text-foreground`, `border-border`, `text-muted-foreground`
   - Mobile-first responsive: single column, appropriate padding on mobile (px-4), comfortable reading width on desktop (max-w-2xl mx-auto)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Homepage renders feed items from database. FilterBar updates URL params on selection. Filters for type and topic work via searchParams. Empty state shown when no items match. ThemeToggle in sticky header. Dark mode works. Mobile-responsive single-column layout.</done>
</task>

<task type="auto">
  <name>Task 2: Item detail page with structured patch-notes layout</name>
  <files>src/app/item/[id]/page.tsx</files>
  <action>
Create `src/app/item/[id]/page.tsx` as an async server component:

1. `generateMetadata` function:
   - `await params` to get id (params is a Promise in Next.js 15+)
   - Fetch item with `getItemById(id)`
   - If not found, return `{ title: 'Not Found - PatchNotes' }`
   - If found, parse summary and use headline for description: `{ title: item.title + ' - PatchNotes', description: parsedSummary?.headline ?? item.title }`

2. Default export `ItemPage`:
   - `await params` to get id
   - Fetch item with `getItemById(id)`
   - If not found, call `notFound()` from `next/navigation` (returns proper HTTP 404)
   - Parse summary with `parseSummary(item.summary)`
   - Layout (patch-notes changelog aesthetic, UI-03):
     - Back link: `<Link href="/">` with arrow icon, "Back to feed"
     - Header section: TypeBadge + date + SeverityBadge (if summary exists)
     - Title: `<h1>` with item.title, large and bold
     - If parsedSummary exists, render structured sections:
       - Headline in larger muted text
       - "What Changed" section with `<h2>` heading and `parsedSummary.whatChanged` content
       - "Who It Affects" section with `<h2>` heading and `parsedSummary.whoAffected` content
       - "Why It Matters" section with `<h2>` heading and `parsedSummary.whyItMatters` content
       - Use a card or bordered section for each block — visually distinct sections
     - If no parsedSummary, show item.title only with a note that summary is unavailable
     - "View Official Source" link: `<a href={item.sourceUrl} target="_blank" rel="noopener noreferrer">` styled as a button with ExternalLink icon from lucide-react (FEED-06)
   - Use `max-w-2xl mx-auto px-4 py-8` for consistent layout with homepage
   - All text uses semantic Tailwind classes for dark mode compatibility
   - Section headings should use monospace-inspired styling to reinforce the changelog metaphor: consider `font-mono text-sm uppercase tracking-wider text-muted-foreground` for section labels
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Detail page at /item/[id] renders full structured breakdown. Shows whatChanged, whoAffected, whyItMatters sections. Links to official source. Returns 404 for missing items. Has SEO metadata. Back link to feed. Dark mode compatible. Changelog aesthetic with section headings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify feed and detail page UI</name>
  <action>
What was built: Complete feed and detail page UI — the first user-facing product. Homepage shows government changes with type/topic filters. Detail page shows structured patch-notes breakdown. Dark mode works throughout.

How to verify:
1. Run `npm run dev` and visit http://localhost:3000
2. Verify the homepage shows feed items (if DB has data) or empty state (if no data)
3. Test type filter dropdown — select "Bills" and verify URL changes to `?type=bill`
4. Test topic filter dropdown — select a topic and verify URL changes
5. Click a feed item — verify it navigates to `/item/[id]` detail page
6. On detail page: verify "What Changed", "Who It Affects", "Why It Matters" sections render
7. Verify "View Official Source" link opens government URL in new tab
8. Click "Back to feed" link — verify it returns to homepage
9. Toggle dark mode — verify feed and detail pages both switch correctly
10. Resize browser to mobile width — verify no broken layouts
11. Visit http://localhost:3000/item/nonexistent-id — verify 404 page
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approves the feed and detail page UI, or provides issues to fix</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` — full test suite passes (including plan 01 query tests)
3. `npx next build` completes without errors
4. Homepage loads at `/` with feed items or empty state
5. Filter dropdowns update URL and filter results
6. Detail page renders at `/item/[id]` with structured breakdown
7. 404 returned for nonexistent item IDs
8. Dark mode works on both pages
9. Mobile layout is clean — no horizontal overflow
</verification>

<success_criteria>
- Homepage replaces splash page with live feed of government changes (FEED-01, FEED-02)
- Type and topic filter dropdowns work via URL params (FEED-03, FEED-04)
- Feed items link to detail page (FEED-05)
- Detail page shows official source link (FEED-06)
- Responsive single-column layout works on mobile and desktop (UI-01)
- Dark mode toggle works on all new pages (UI-02)
- Patch-notes changelog aesthetic used consistently (UI-03)
- No loading spinners — data comes from pre-generated summaries in DB (UI-04)
</success_criteria>

<output>
After completion, create `.planning/phases/04-digest-feed/04-02-SUMMARY.md`
</output>
