---
phase: 06-user-accounts-personalization
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - "src/app/(auth)/sign-up/page.tsx"
  - "src/app/(auth)/sign-in/page.tsx"
  - src/app/onboarding/topics/page.tsx
  - src/components/header-auth.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03
  - PERS-01
must_haves:
  truths:
    - "A new user can register with name, email, and password on the sign-up page"
    - "After registration, user is automatically redirected to the topic onboarding page"
    - "User can pick topics on the onboarding page and save them, then gets redirected to homepage"
    - "An existing user can log in with email and password on the sign-in page"
    - "A logged-in user sees their email and a sign-out button in the header"
    - "A signed-out user sees a Sign In link in the header"
    - "After sign-out, user is redirected to the homepage and header shows Sign In again"
    - "Refreshing the browser after login keeps the session alive (USER-02)"
  artifacts:
    - path: "src/app/(auth)/sign-up/page.tsx"
      provides: "Registration form page"
    - path: "src/app/(auth)/sign-in/page.tsx"
      provides: "Login form page"
    - path: "src/app/onboarding/topics/page.tsx"
      provides: "Post-registration topic picker"
    - path: "src/components/header-auth.tsx"
      provides: "Auth-aware header section (session state, sign-out)"
  key_links:
    - from: "src/app/(auth)/sign-up/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signUp.email({ callbackURL: '/onboarding/topics' })"
      pattern: "signUp\\.email"
    - from: "src/app/(auth)/sign-in/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signIn.email({ callbackURL: '/' })"
      pattern: "signIn\\.email"
    - from: "src/app/onboarding/topics/page.tsx"
      to: "src/features/auth/queries.ts"
      via: "saveTopics server action"
      pattern: "saveTopics"
    - from: "src/components/header-auth.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.useSession() and authClient.signOut()"
      pattern: "useSession"
---

<objective>
Build the auth UI pages (sign-up, sign-in) and topic onboarding flow, plus update the header to show auth state. After this plan, a user can register, pick topics, log in, log out, and see their session reflected in the header.

Purpose: Delivers the complete auth user journey — the critical path from anonymous visitor to authenticated user with topic preferences.
Output: Working sign-up/sign-in pages, topic onboarding page, auth-aware header.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-accounts-personalization/06-RESEARCH.md
@.planning/phases/06-user-accounts-personalization/06-01-SUMMARY.md

@src/app/page.tsx
@src/components/theme-toggle.tsx
@src/features/feed/options.ts

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/auth-client.ts:
```typescript
export const authClient: ReturnType<typeof createAuthClient>;
// authClient.signUp.email({ email, password, name, callbackURL })
// authClient.signIn.email({ email, password, callbackURL })
// authClient.signOut({ fetchOptions: { onSuccess } })
// authClient.useSession() -> { data: session, isPending }
```

From src/lib/auth.ts:
```typescript
export const auth: ReturnType<typeof betterAuth>;
// auth.api.getSession({ headers: await headers() }) -> { user: { id, email, name } } | null
```

From src/features/auth/queries.ts:
```typescript
export async function saveTopics(topics: string[]): Promise<void>; // redirects to /
export async function updateTopics(topics: string[]): Promise<void>; // revalidates /profile, no redirect
export async function getUserTopics(userId: string): Promise<string[]>;
```

From src/features/feed/options.ts:
```typescript
export const TOPIC_OPTIONS: Array<{ value: string; label: string }>;
// Reuse for onboarding topic picker — filter out 'all' sentinel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sign-up page, sign-in page, and topic onboarding page</name>
  <files>
    src/app/(auth)/sign-up/page.tsx,
    src/app/(auth)/sign-in/page.tsx,
    src/app/onboarding/topics/page.tsx
  </files>
  <action>
1. Create `src/app/(auth)/sign-up/page.tsx` — a "use client" page:
   - Form with name, email, password fields (styled with Tailwind, consistent with app design)
   - On submit: call `authClient.signUp.email({ email, password, name, callbackURL: "/onboarding/topics" })`
   - Show error message if signUp returns error (e.g., "Email already in use")
   - Link at bottom: "Already have an account? Sign in" linking to /sign-in
   - Centered card layout, max-w-sm, with PatchNotes branding at top
   - Password minimum 8 characters (minLength on input + note in UI)

2. Create `src/app/(auth)/sign-in/page.tsx` — a "use client" page:
   - Form with email, password fields
   - On submit: call `authClient.signIn.email({ email, password, callbackURL: "/" })`
   - Show error message on failure (e.g., "Invalid email or password")
   - Link at bottom: "Don't have an account? Sign up" linking to /sign-up
   - Same centered card styling as sign-up

3. Create `src/app/onboarding/topics/page.tsx` — a Server Component wrapper with client form:
   - Server component checks session via `auth.api.getSession({ headers: await headers() })` — redirect to /sign-in if not authenticated
   - Renders a client component `TopicPicker` that shows all topics from TOPIC_OPTIONS (filter out the 'all' sentinel)
   - Each topic is a toggleable button/chip (selected state with filled bg, unselected with border)
   - "Continue" button calls the `saveTopics` server action with selected topic values
   - After saveTopics, redirect to "/" (saveTopics already calls redirect)
   - Allow skipping (0 topics selected = show all items on feed, which is the correct fallback per research pitfall #6)
   - Header text: "What topics interest you?" with subtitle "Pick the topics you want to follow. You can change these later."
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Sign-up page at /sign-up collects name/email/password and calls authClient.signUp.email. Sign-in page at /sign-in collects email/password and calls authClient.signIn.email. Onboarding page at /onboarding/topics shows topic chips, saves via server action, redirects to homepage. All three pages build without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth-aware header with session state and sign-out</name>
  <files>
    src/components/header-auth.tsx,
    src/app/page.tsx
  </files>
  <action>
1. Create `src/components/header-auth.tsx` — a "use client" component:
   - Uses `authClient.useSession()` to get session state
   - If `isPending`: show a small skeleton/placeholder (prevents layout shift)
   - If no session: show a "Sign in" link (href="/sign-in") styled as a subtle button
   - If session exists: show user's name or email truncated + a "Sign out" button
   - Sign out calls `authClient.signOut({ fetchOptions: { onSuccess: () => router.push("/") } })` using `useRouter`
   - Optionally show a link to "/profile" (profile icon or "Profile" text) when logged in
   - Keep it compact — this sits in the header bar next to the ThemeToggle

2. Update `src/app/page.tsx` header section:
   - Import HeaderAuth component
   - Add `<HeaderAuth />` next to or near `<ThemeToggle />` in the header
   - The header should show: PatchNotes title | [HeaderAuth] [ThemeToggle]
   - Wrap HeaderAuth in Suspense if needed for SSR safety (it uses client hooks)

3. Verify that the header renders correctly for both authenticated and unauthenticated states by running the build.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Header shows "Sign in" link when logged out, shows user info + "Sign out" button when logged in. Sign-out redirects to homepage. Page builds cleanly.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Auth pages exist at /sign-up, /sign-in, /onboarding/topics
- Header component uses authClient.useSession() for reactive session state
- Sign-up redirects to onboarding, sign-in redirects to homepage
- Topic picker uses TOPIC_OPTIONS from existing feed options
</verification>

<success_criteria>
- A new user can navigate to /sign-up, create an account, and be redirected to topic onboarding
- User can pick topics on onboarding page and land on the homepage
- An existing user can sign in at /sign-in and land on the homepage
- Header reflects auth state: "Sign in" when logged out, user info + "Sign out" when logged in
- Session persists across browser refresh (USER-02 — better-auth database sessions handle this)
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-accounts-personalization/06-02-SUMMARY.md`
</output>
